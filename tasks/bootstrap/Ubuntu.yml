---

- tags: [ bootstrap ]
  block:

    - name: "install shim packages"
      apt:
        update_cache: yes
        cache_valid_time: 3600
        install_recommends: no
        name:
          - python-pip
          - python-mysqldb
          - unzip
          - git
          - curl
          - python-setuptools
          - net-tools
