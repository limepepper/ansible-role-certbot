---

- tags: main-run
  become: True
  block:

  - name: install epel
    package:
      name:
        - epel-release

  - name: install shim packages
    package:
      name:
        - libselinux-python
        - selinux-policy
        - python-pip
        - unzip
        - git
        - lsof
        - net-tools
        - curl
